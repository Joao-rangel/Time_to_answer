<!-- Modal -->
<div class="modal fade" id="avatarModal" tabindex="-1" role="dialog" aria-labelledby="avatarModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <%= form_with(url: users_backoffice_profile_path, model: @user, class:"form-horizontal form-material") do |form| %>
        <div class="modal-header">
          <h5 class="modal-title" id="ModalLabel">Alterar imagem de perfil</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <%= form.fields_for :user_profile do |profile_form| %>
            <div class="form-group text-center">
              <%= image_tag set_avatar, alt:"user_profile", class:"img-circle m-3", width:"250", id:"avatarImgModal" %>
              <%= profile_form.file_field :avatar %>
            </div>
          <% end %>
        </div>°
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Fechar</button>
          <%= form.submit "Salvar", class:"btn btn-info" %>
        </div>
      <% end %>
    </div>
  </div>
</div>